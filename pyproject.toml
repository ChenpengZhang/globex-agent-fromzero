[project]
name = "globex-agent-fromzero"
version = "0.1.0"
description = "从零构建的跨境电商 Agent 学习项目"
requires-python = ">=3.11,<3.14"
dependencies = [
    "agentscope>=2,<3",
    "python-dotenv>=1.2",
]

[tool.uv.workspace]
members = []
